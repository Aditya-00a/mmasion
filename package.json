{
  "name": "mmasion",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first supervisor gateway for routing, verification, consensus, and conversational HITL.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
